version: v1.0
name: Release to APT

agent:
  machine:
    type: s1-prod-ubuntu20-04-amd64-1

execution_time_limit:
  hours: 1

blocks:
  - name: linux/amd64
    task:
      jobs:
        - name: linux/amd64
          commands:
            - echo "Releasing for Linux!"