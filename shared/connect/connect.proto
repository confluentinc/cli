syntax = "proto3";

package connect;

import "kafka/core/v1/core.proto";
import "kafka/scheduler/v1/scheduler.proto";

message ListRequest {    
}

message ListResponse {
  kafka.core.v1.Error error = 1; // TODO: use this error now
  repeated kafka.scheduler.v1.ConnectCluster clusters = 2;
}

service Connect {
  rpc List(ListRequest) returns (ListResponse) {}
  // ...
}
