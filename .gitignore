bin/
dist/

# TODO: figure out how to integrate our CLI command reference docs into https://github.com/confluentinc/docs
# We don't want them stored in this repo, as per Ewen (https://confluent.slack.com/archives/C9Y6NAM6X/p1553233699097000?thread_ts=1553219127.096100&cid=C9Y6NAM6X)
docs/**/*.rst

.semaphore-cache/
git-setup-semaphore.sh

# Binaries for programs
*.exe
*.dll
*.so
*.dylib
.idea

# Test binary, build with `go test -c`
*.test

# Output of the go coverage tool, specifically when used with LiteIDE
*.out

# Project-local glide cache, RE: https://github.com/Masterminds/glide/issues/736
vendor/*
!vendor/vendor.json

# Terraform stuff
*.tfstate*
terraform.d
.terraform/
.netrc

coverage.txt
/ccloud
*.sw*
.vscode

# If we don't check go.sum into git, then CI can't complain that it has conflicting shasums
go.sum
