// Code generated by cli-terraform-generator; DO NOT EDIT.

package testserver

import (
	"encoding/json"
	"net/http"
	"os"
	"path/filepath"
	"testing"

	"github.com/gorilla/mux"
	"github.com/stretchr/testify/require"

	notificationsv1 "github.com/confluentinc/ccloud-sdk-go-v2/notifications/v1"
)

// Handler for "/notifications/v1/resource-subscriptions"
func handleNotificationsV1ResourceSubscriptions(t *testing.T) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		switch r.Method {
		case http.MethodPost:
			resourceSubscription := readNotificationsV1ResourceSubscriptionFile(t, "create_resource_subscription.json")

			// Overwrite updated fields using the request body
			err := json.NewDecoder(r.Body).Decode(&resourceSubscription)
			require.NoError(t, err)

			err = json.NewEncoder(w).Encode(resourceSubscription)
			require.NoError(t, err)
		}
	}
}

// Handler for "/notifications/v1/resource-subscriptions/{id}"
func handleNotificationsV1ResourceSubscriptionsId(t *testing.T) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		id := mux.Vars(r)["id"]
		switch r.Method {
		case http.MethodGet:
			switch id {
			case "invalid":
				w.WriteHeader(http.StatusNotFound)
			default:
				resourceSubscription := readNotificationsV1ResourceSubscriptionFile(t, "read_created_resource_subscription.json")

				err := json.NewEncoder(w).Encode(&resourceSubscription)
				require.NoError(t, err)
			}
		case http.MethodPatch:
			switch id {
			case "invalid":
				w.WriteHeader(http.StatusNotFound)
			default:
				resourceSubscription := readNotificationsV1ResourceSubscriptionFile(t, "read_created_resource_subscription.json")

				// Overwrite updated fields using the request body
				err := json.NewDecoder(r.Body).Decode(&resourceSubscription)
				require.NoError(t, err)

				err = json.NewEncoder(w).Encode(resourceSubscription)
				require.NoError(t, err)
			}
		case http.MethodDelete:
			switch id {
			case "invalid":
				w.WriteHeader(http.StatusNotFound)
			default:
				w.WriteHeader(http.StatusNoContent)
			}
		}
	}
}

func readNotificationsV1ResourceSubscriptionFile(t *testing.T, filename string) notificationsv1.NotificationsV1ResourceSubscription {
	jsonPath := filepath.Join("test", "fixtures", "input", "notifications", "resource-subscription", filename)
	jsonData, err := os.ReadFile(jsonPath)
	require.NoError(t, err)

	resourceSubscription := notificationsv1.NotificationsV1ResourceSubscription{}
	err = json.Unmarshal(jsonData, &resourceSubscription)
	require.NoError(t, err)

	return resourceSubscription
}
