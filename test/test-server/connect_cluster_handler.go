// Code generated by cli-terraform-generator; DO NOT EDIT.

package testserver

import (
	"encoding/json"
	"net/http"
	"os"
	"path/filepath"
	"testing"

	"github.com/gorilla/mux"
	"github.com/stretchr/testify/require"

	usmv1 "github.com/confluentinc/ccloud-sdk-go-v2/usm/v1"
)

// Handler for "/usm/v1/connect-clusters"
func handleUsmV1ConnectClusters(t *testing.T) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		switch r.Method {
		case http.MethodGet:
			connectCluster := readUsmV1ConnectClusterFile(t, "read_created_connect_cluster.json")

			connectClusterList := &usmv1.UsmV1ConnectClusterList{
				Data: []usmv1.UsmV1ConnectCluster{connectCluster},
			}

			err := json.NewEncoder(w).Encode(connectClusterList)
			require.NoError(t, err)
		case http.MethodPost:
			connectCluster := readUsmV1ConnectClusterFile(t, "create_connect_cluster.json")

			// Overwrite updated fields using the request body
			err := json.NewDecoder(r.Body).Decode(&connectCluster)
			require.NoError(t, err)

			err = json.NewEncoder(w).Encode(connectCluster)
			require.NoError(t, err)
		}
	}
}

// Handler for "/usm/v1/connect-clusters/{id}"
func handleUsmV1ConnectClustersId(t *testing.T) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		id := mux.Vars(r)["id"]
		switch r.Method {
		case http.MethodGet:
			switch id {
			case "invalid":
				w.WriteHeader(http.StatusNotFound)
			default:
				connectCluster := readUsmV1ConnectClusterFile(t, "read_created_connect_cluster.json")

				err := json.NewEncoder(w).Encode(&connectCluster)
				require.NoError(t, err)
			}
		case http.MethodDelete:
			switch id {
			case "invalid":
				w.WriteHeader(http.StatusNotFound)
			default:
				w.WriteHeader(http.StatusNoContent)
			}
		}
	}
}

func readUsmV1ConnectClusterFile(t *testing.T, filename string) usmv1.UsmV1ConnectCluster {
	jsonPath := filepath.Join("test", "fixtures", "input", "usm", "connect_cluster", filename)
	jsonData, err := os.ReadFile(jsonPath)
	require.NoError(t, err)

	connectCluster := usmv1.UsmV1ConnectCluster{}
	err = json.Unmarshal(jsonData, &connectCluster)
	require.NoError(t, err)

	return connectCluster
}
