// Code generated by cli-terraform-generator; DO NOT EDIT.

package testserver

import (
	"encoding/json"
	"net/http"
	"os"
	"path/filepath"
	"testing"

	"github.com/gorilla/mux"
	"github.com/stretchr/testify/require"

	notificationsv1 "github.com/confluentinc/ccloud-sdk-go-v2-internal/notifications/v1"
)

// Handler for "/notifications/v1/user-notifications"
func handleNotificationsV1UserNotifications(t *testing.T) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		switch r.Method {
		case http.MethodGet:
			userNotification := readNotificationsV1UserNotificationFile(t, "read_created_user_notification.json")

			userNotificationList := &notificationsv1.NotificationsV1UserNotificationList{
				Data: []notificationsv1.NotificationsV1UserNotification{userNotification},
			}

			err := json.NewEncoder(w).Encode(userNotificationList)
			require.NoError(t, err)
		}
	}
}

// Handler for "/notifications/v1/user-notifications/{id}"
func handleNotificationsV1UserNotificationsId(t *testing.T) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		id := mux.Vars(r)["id"]
		switch r.Method {
		case http.MethodGet:
			switch id {
			case "invalid":
				w.WriteHeader(http.StatusNotFound)
			default:
				userNotification := readNotificationsV1UserNotificationFile(t, "read_created_user_notification.json")

				err := json.NewEncoder(w).Encode(&userNotification)
				require.NoError(t, err)
			}
		case http.MethodPatch:
			switch id {
			case "invalid":
				w.WriteHeader(http.StatusNotFound)
			default:
				userNotification := readNotificationsV1UserNotificationFile(t, "read_created_user_notification.json")

				// Overwrite updated fields using the request body
				err := json.NewDecoder(r.Body).Decode(&userNotification)
				require.NoError(t, err)

				err = json.NewEncoder(w).Encode(userNotification)
				require.NoError(t, err)
			}
		}
	}
}

func readNotificationsV1UserNotificationFile(t *testing.T, filename string) notificationsv1.NotificationsV1UserNotification {
	jsonPath := filepath.Join("test", "fixtures", "input", "notifications", "user-notification", filename)
	jsonData, err := os.ReadFile(jsonPath)
	require.NoError(t, err)

	userNotification := notificationsv1.NotificationsV1UserNotification{}
	err = json.Unmarshal(jsonData, &userNotification)
	require.NoError(t, err)

	return userNotification
}
