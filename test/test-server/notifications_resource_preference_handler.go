// Code generated by cli-terraform-generator; DO NOT EDIT.

package testserver

import (
	"encoding/json"
	"net/http"
	"os"
	"path/filepath"
	"testing"

	"github.com/gorilla/mux"
	"github.com/stretchr/testify/require"

	notificationsv1 "github.com/confluentinc/ccloud-sdk-go-v2-internal/notifications/v1"
)

// Handler for "/notifications/v1/resource-preferences"
func handleNotificationsV1ResourcePreferences(t *testing.T) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		switch r.Method {
		case http.MethodPost:
			resourcePreference := readNotificationsV1ResourcePreferenceFile(t, "create_resource_preference.json")

			// Overwrite updated fields using the request body
			err := json.NewDecoder(r.Body).Decode(&resourcePreference)
			require.NoError(t, err)

			err = json.NewEncoder(w).Encode(resourcePreference)
			require.NoError(t, err)
		}
	}
}

// Handler for "/notifications/v1/resource-preferences/{id}"
func handleNotificationsV1ResourcePreferencesId(t *testing.T) http.HandlerFunc {
	return func(w http.ResponseWriter, r *http.Request) {
		id := mux.Vars(r)["id"]
		switch r.Method {
		case http.MethodGet:
			switch id {
			case "invalid":
				w.WriteHeader(http.StatusNotFound)
			default:
				resourcePreference := readNotificationsV1ResourcePreferenceFile(t, "read_created_resource_preference.json")

				err := json.NewEncoder(w).Encode(&resourcePreference)
				require.NoError(t, err)
			}
		case http.MethodPatch:
			switch id {
			case "invalid":
				w.WriteHeader(http.StatusNotFound)
			default:
				resourcePreference := readNotificationsV1ResourcePreferenceFile(t, "read_created_resource_preference.json")

				// Overwrite updated fields using the request body
				err := json.NewDecoder(r.Body).Decode(&resourcePreference)
				require.NoError(t, err)

				err = json.NewEncoder(w).Encode(resourcePreference)
				require.NoError(t, err)
			}
		case http.MethodDelete:
			switch id {
			case "invalid":
				w.WriteHeader(http.StatusNotFound)
			default:
				w.WriteHeader(http.StatusNoContent)
			}
		}
	}
}

func readNotificationsV1ResourcePreferenceFile(t *testing.T, filename string) notificationsv1.NotificationsV1ResourcePreference {
	jsonPath := filepath.Join("test", "fixtures", "input", "notifications", "resource-preference", filename)
	jsonData, err := os.ReadFile(jsonPath)
	require.NoError(t, err)

	resourcePreference := notificationsv1.NotificationsV1ResourcePreference{}
	err = json.Unmarshal(jsonData, &resourcePreference)
	require.NoError(t, err)

	return resourcePreference
}
