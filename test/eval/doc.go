// Package eval is the multi-agent state-isolation eval harness. It is gated behind the `eval`
// build tag so it never runs under `make test`; run it with `go test -tags eval ./test/eval/...`.
package eval
