Manage custom code logging.

Usage:
  confluent custom-code-logging [command]

Available Commands:
  create      Create a custom code logging.
  delete      Delete one or more custom code loggings.
  describe    Describe a custom code logging.
  list        List custom code loggings.
  update      Update a custom code logging.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent custom-code-logging [command] --help" for more information about a command.
