Manage AsyncAPI document tooling.

Usage:
  confluent asyncapi [command]

Available Commands:
  export      Export an AsyncAPI specification.
  import      Import an AsyncAPI specification.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent asyncapi [command] --help" for more information about a command.
