Manage Confluent Cloud CCPM custom Connect plugins.

Usage:
  confluent ccpm plugin [command]

Available Commands:
  create      Create a CCPM custom Connect plugin.
  delete      Delete one or more CCPM custom Connect plugins.
  describe    Describe a CCPM custom Connect plugin.
  list        List CCPM custom Connect plugins.
  update      Update an existing CCPM custom Connect plugin.
  version     Manage custom Connect plugin versions.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent ccpm plugin [command] --help" for more information about a command.
