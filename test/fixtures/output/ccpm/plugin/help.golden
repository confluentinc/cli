Manage custom Connect plugins.

Usage:
  confluent ccpm plugin [command]

Available Commands:
  create      Create a custom Connect plugin.
  delete      Delete one or more custom Connect plugins.
  describe    Describe a custom Connect plugin.
  list        List custom Connect plugins.
  update      Update a custom Connect plugin.
  version     Manage custom Connect plugin versions.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent ccpm plugin [command] --help" for more information about a command.
