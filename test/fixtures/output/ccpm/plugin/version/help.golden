Manage custom Connect plugin versions.

Usage:
  confluent ccpm plugin version [command]

Available Commands:
  create      Create a custom Connect plugin version.
  delete      Delete one or more custom Connect plugin versions.
  describe    Describe a custom Connect plugin version.
  list        List custom Connect plugin versions.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent ccpm plugin version [command] --help" for more information about a command.
