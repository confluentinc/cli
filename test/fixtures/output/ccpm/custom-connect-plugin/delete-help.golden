Delete one or more CCPM custom Connect plugins.

Usage:
  confluent ccpm custom-connect-plugin delete <id-1> [id-2] ... [id-n] [flags]

Examples:
Delete a custom Connect plugin by ID.

  $ confluent ccpm custom-connect-plugin delete ccp-123456 --environment env-12345

Force delete a custom Connect plugin without confirmation.

  $ confluent ccpm custom-connect-plugin delete ccp-123456 --environment env-12345 --force

Flags:
      --environment string   Environment ID.
      --context string       CLI context name.
      --force                Skip the deletion confirmation prompt.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).
