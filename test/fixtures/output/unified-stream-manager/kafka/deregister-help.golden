Deregister Kafka clusters.

Usage:
  confluent unified-stream-manager kafka deregister <usm-cluster-id-1> [usm-cluster-id-2] [usm-cluster-id-3] ... [usm-cluster-id-n] [flags]

Examples:
Deregister a Confluent Platform Kafka cluster.

  $ confluent unified-stream-manager kafka deregister usmkc-abc123

Flags:
      --environment string   Environment ID.
      --force                Skip the deletion confirmation prompt.
      --context string       CLI context name.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).
