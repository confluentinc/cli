Error: required flag(s) "region" not set
Usage:
  confluent unified-stream-manager kafka register <confluent-platform-kafka-cluster-id> [flags]

Examples:
Register a Confluent Platform Kafka cluster with the ID 4k0R9d1GTS5tI9f4Y2xZ0Q.

  $ confluent unified-stream-manager kafka register 4k0R9d1GTS5tI9f4Y2xZ0Q --name my-kafka-cluster --cloud aws --region us-east-1

Flags:
      --name string          REQUIRED: Name of the USM Kafka cluster.
      --cloud string         REQUIRED: Specify the cloud provider as "aws", "azure", or "gcp".
      --region string        REQUIRED: Specify the cloud region.
      --environment string   Environment ID.
      --context string       CLI context name.
  -o, --output string        Specify the output format as "human", "json", or "yaml". (default "human")

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

