Manage USM Kafka clusters.

Usage:
  confluent unified-stream-manager kafka [command]

Available Commands:
  deregister  Deregister Kafka clusters.
  describe    Describe a Kafka cluster.
  list        List Kafka clusters.
  register    Register a Kafka cluster.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent unified-stream-manager kafka [command] --help" for more information about a command.
