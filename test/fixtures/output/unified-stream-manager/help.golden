Manage Unified Stream Manager clusters.

Usage:
  confluent unified-stream-manager [command]

Aliases:
  unified-stream-manager, usm

Available Commands:
  connect     Manage USM Connect clusters.
  kafka       Manage USM Kafka clusters.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent unified-stream-manager [command] --help" for more information about a command.
