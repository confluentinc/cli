Manage Role-Based Access Control (RBAC) and Identity and Access Management (IAM) role bindings.

Usage:
  confluent iam rbac role-binding [command]

Aliases:
  role-binding, rb

Available Commands:
  create      Create a role binding.
  delete      Delete a role binding.
  list        List role bindings.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which may contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent iam rbac role-binding [command] --help" for more information about a command.
