Manage Role-Based Access Control (RBAC) permissions.

Usage:
  confluent iam rbac [command]

Available Commands:
  role         Manage RBAC and IAM roles.
  role-binding Manage RBAC and IAM role bindings.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which may contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent iam rbac [command] --help" for more information about a command.
