Manage service accounts.

Usage:
  confluent iam service-account [command]

Aliases:
  service-account, sa

Available Commands:
  create      Create a service account.
  delete      Delete a service account.
  describe    Describe a service account.
  list        List service accounts.
  update      Update a service account.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent iam service-account [command] --help" for more information about a command.
