Manage certificate pools.

Usage:
  confluent iam certificate-pool [command]

Available Commands:
  create      Create a certificate pool.
  delete      Delete one or more certificate pools.
  describe    Describe a certificate pool.
  list        List certificate pools.
  update      Update a certificate pool.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent iam certificate-pool [command] --help" for more information about a command.
