Delete one or more certificate pools.

Usage:
  confluent iam certificate-pool delete <id-1> [id-2] ... [id-n] [flags]

Examples:
Delete certificate pool "pool-123":

  $ confluent iam certificate-pool delete pool-123 --provider provider-123

Flags:
      --provider string   REQUIRED: ID of this pool's certificate authority.
      --force             Skip the deletion confirmation prompt.
      --context string    CLI context name.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).
