Manage identity providers.

Usage:
  confluent iam provider [command]

Available Commands:
  create      Create an identity provider.
  delete      Delete an identity provider.
  describe    Describe an identity provider.
  list        List identity providers.
  update      Update an identity provider.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent iam provider [command] --help" for more information about a command.
