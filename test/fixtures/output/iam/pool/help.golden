Manage identity pools.

Usage:
  confluent iam pool [command]

Available Commands:
  create      Create an identity pool.
  delete      Delete identity pools.
  describe    Describe an identity pool.
  list        List identity pools.
  update      Update an identity pool.
  use         Choose an identity pool to be used in subsequent commands.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent iam pool [command] --help" for more information about a command.
