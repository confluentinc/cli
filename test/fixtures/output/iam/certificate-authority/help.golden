Manage Confluent Cloud IAM certificate authorities.

Usage:
  confluent iam certificate-authority [command]

Available Commands:
  create      Create an IAM certificate authority.
  delete      Delete one or more IAM certificate authorities.
  describe    Describe an IAM certificate authority.
  list        List IAM certificate authorities.
  update      Update an existing IAM certificate authority.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent iam certificate-authority [command] --help" for more information about a command.
