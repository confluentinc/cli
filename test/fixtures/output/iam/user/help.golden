Manage users.

Usage:
  confluent iam user [command]

Available Commands:
  delete      Delete users from your organization.
  describe    Describe a user.
  invitation  Manage invitations.
  list        List an organization's users.
  update      Update a user.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent iam user [command] --help" for more information about a command.
