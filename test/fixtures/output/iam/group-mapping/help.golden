Manage SSO group mappings.

Usage:
  confluent iam group-mapping [command]

Available Commands:
  create      Create a group mapping.
  delete      Delete one or more group mappings.
  describe    Describe a group mapping.
  list        List group mappings.
  update      Update a group mapping.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent iam group-mapping [command] --help" for more information about a command.
