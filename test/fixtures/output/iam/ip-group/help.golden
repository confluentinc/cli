Manage IP groups and their permissions.

Usage:
  confluent iam ip-group [command]

Available Commands:
  create      Create an IP group.
  delete      Delete an IP group.
  describe    Describe an IP group.
  list        List IP groups.
  update      Update an IP group.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent iam ip-group [command] --help" for more information about a command.
