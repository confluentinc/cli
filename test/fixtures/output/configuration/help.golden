Configure the Confluent CLI.

Usage:
  confluent configuration [command]

Aliases:
  configuration, config

Available Commands:
  set         Set a user-configurable field's value.
  view        View user-configurable fields in ~/.confluent/config.json.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent configuration [command] --help" for more information about a command.
