Set a field's value in ~/.confluent/config.json.

Usage:
  confluent configuration set <config-field-1=value-1> ... [config-field-n=value-n] [flags]

Examples:
Disable plugins by setting "disable_plugins" to true

  $ confluent configuration set disable_plugins=true

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).
