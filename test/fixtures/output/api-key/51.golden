Error: API key "MYKEY2" and associated Kafka cluster are not stored in local CLI state

Suggestions:
    If you did not create this API key with the CLI or created it on another computer, you must first store the API key and secret locally with `confluent api-key store MYKEY2 <secret>`.
