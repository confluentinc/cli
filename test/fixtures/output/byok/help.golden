Manage your keys in Confluent Cloud.

Usage:
  confluent byok [command]

Available Commands:
  create      Register a self-managed encryption key.
  delete      Delete self-managed keys.
  describe    Describe a self-managed key.
  list        List self-managed keys.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which may contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent byok [command] --help" for more information about a command.
