List self-managed keys registered in Confluent Cloud.

Usage:
  confluent byok list [flags]

Flags:
      --cloud string              Specify the cloud provider as "aws", "azure", or "gcp".
      --state string              Specify the state as "in-use" or "available".
      --region string             Filter by region.
      --validation-phase string   Specify the validation phase as "valid", "invalid", or "initializing".
      --display-name string       Filter by display name.
      --key string                Filter by key identifier.
  -o, --output string             Specify the output format as "human", "json", or "yaml". (default "human")

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).
