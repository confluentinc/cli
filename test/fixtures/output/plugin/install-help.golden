Install official Confluent CLI plugins from the confluentinc/cli-plugins repository.

Usage:
  confluent plugin install [flags]

Flags:
      --plugin-directory string   The plugin installation directory. This must be a directory in your $PATH. If not specified, the default is the CLI installation directory for Windows, and "usr/local/bin" for MacOS and Linux.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).
