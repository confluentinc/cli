Manage Flink Identity and Access Management (IAM) bindings.

Usage:
  confluent flink iam-binding [command]

Available Commands:
  create      Create a Flink IAM binding.
  delete      Delete Flink IAM bindings.
  list        List Flink IAM bindings.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent flink iam-binding [command] --help" for more information about a command.
