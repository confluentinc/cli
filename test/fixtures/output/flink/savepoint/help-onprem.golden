Manage Flink savepoint.

Usage:
  confluent flink savepoint [command]

Available Commands:
  create      Create a Flink Savepoint.
  delete      Delete Flink Savepoint in Confluent Platform.
  describe    Describe flink savepoint in Confluent Platform.
  detach      Detach Flink Savepoint in Confluent Platform.
  list        List Flink SQL statements in Confluent Platform.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent flink savepoint [command] --help" for more information about a command.
