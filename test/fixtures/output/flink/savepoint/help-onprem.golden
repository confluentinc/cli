Manage Flink savepoints.

Usage:
  confluent flink savepoint [command]

Available Commands:
  create      Create a Flink savepoint.
  delete      Delete Flink savepoint in Confluent Platform.
  describe    Describe a Flink savepoint in Confluent Platform.
  detach      Detach a Flink savepoint in Confluent Platform.
  list        List Flink savepoints in Confluent Platform.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent flink savepoint [command] --help" for more information about a command.
