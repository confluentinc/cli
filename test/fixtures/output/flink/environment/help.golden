Manage Flink environments.

Usage:
  confluent flink environment [command]

Aliases:
  environment, env

Available Commands:
  create      Create a Flink environment.
  delete      Delete one or more Flink environments.
  describe    Describe a Flink environment.
  list        List Flink environments.
  update      Update a Flink environment.
  use         Use an environment as default for Flink applications

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent flink environment [command] --help" for more information about a command.
