Create a Flink SQL statement.

Usage:
  confluent flink statement create <name> [flags]

Flags:
      --name string            Name of the statement. (default "<randomly-generated-id>")
      --database string        The database against which the statement will run. For example, the display name of a Kafka cluster. (default "<current-kafka-cluster>")
      --compute-pool string    Flink compute pool ID.
      --identity-pool string   Identity pool ID.
      --environment string     Environment ID.
      --context string         CLI context name.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).
