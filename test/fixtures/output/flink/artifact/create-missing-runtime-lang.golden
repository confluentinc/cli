Error: required flag(s) "runtime-language" not set
Usage:
  confluent flink artifact create <name> [flags]

Examples:
Create Flink artifact "my-flink-artifact".

  $ confluent flink artifact create my-flink-artifact --artifact-file plugin.jar --runtime-language java

Flags:
      --artifact-file string      REQUIRED: Flink artifact JAR/ZIP file.
      --runtime-language string   REQUIRED: Specify the artifact runtime language as "java" or "python".
      --description string        Description of Flink artifact.
      --context string            CLI context name.
  -o, --output string             Specify the output format as "human", "json", or "yaml". (default "human")

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

