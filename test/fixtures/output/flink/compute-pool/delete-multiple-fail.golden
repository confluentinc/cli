Error: Flink compute pool "lfcp-654321" not found

Suggestions:
    List available Flink compute pools with `confluent flink compute-pool list` and make sure you have selected the compute pool's environment with `confluent environment use env-123`.
