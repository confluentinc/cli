Update a Flink compute pool config.

Usage:
  confluent flink compute-pool-config update [flags]

Examples:
Update CFU count of a Flink compute pool config.

  $ confluent flink compute-pool update --max-cfu 5

Flags:
      --max-cfu int32        Maximum number of Confluent Flink Units (CFUs) that default compute pools in this organization should auto-scale to. (default -1)
      --default-pool         Whether default compute pools are enabled for the organization
      --environment string   Environment ID.
  -o, --output string        Specify the output format as "human", "json", or "yaml". (default "human")

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).
