Manage Alertmanager.

Usage:
  confluent local services alertmanager [command]

Available Commands:
  log         Print logs showing Alertmanager output.
  start       Start Alertmanager.
  status      Check if Alertmanager is running.
  stop        Stop Alertmanager.
  top         View resource usage for Alertmanager.
  version     Print the current version of Alertmanager.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent local services alertmanager [command] --help" for more information about a command.
