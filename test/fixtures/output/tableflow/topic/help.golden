Manage Tableflow topics.

Usage:
  confluent tableflow topic [command]

Available Commands:
  describe    Describe a topic.
  disable     Disable topics.
  enable      Enable a topic.
  list        List Tableflow topics related to one Kafka cluster.
  update      Update a topic.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent tableflow topic [command] --help" for more information about a command.
