Use this command to manage Schema Registry cloud regions.

Usage:
  confluent schema-registry region [command]

Available Commands:
  list        List Schema Registry cloud regions.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent schema-registry region [command] --help" for more information about a command.
