List references by schema ID, or by subject and version.

Usage:
  confluent schema-registry schema reference list [id] [flags]

Examples:
List references for the schema with ID "1337".

  $ confluent schema-registry schema reference list 1337

List references for the schema with subject "payments" and version "latest".

  $ confluent schema-registry schema reference list --subject payments --version latest

Flags:
      --subject string       Subject of the schema.
      --version string       Version of the schema. Can be a specific version or "latest".
      --context string       CLI context name.
      --environment string   Environment ID.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).
