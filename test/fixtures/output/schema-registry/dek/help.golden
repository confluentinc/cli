Manage Schema Registry Data Encryption Keys (DEKs).

Usage:
  confluent schema-registry dek [command]

Available Commands:
  create      Create a Data Encryption Key (DEK).
  delete      Delete a Data Encryption Key (DEK).
  describe    Describe a Data Encryption Key (DEK).
  subject     Manage Schema Registry Data Encryption Key (DEK) subjects.
  undelete    Undelete a Data Encryption Key (DEK).
  version     Manage Schema Registry Data Encryption Key (DEK) versions.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent schema-registry dek [command] --help" for more information about a command.
