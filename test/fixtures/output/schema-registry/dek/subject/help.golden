Manage Schema Registry Data Encryption Key (DEK) subjects.

Usage:
  confluent schema-registry dek subject [command]

Available Commands:
  list        List Schema Registry Data Encryption Key (DEK) subjects.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent schema-registry dek subject [command] --help" for more information about a command.
