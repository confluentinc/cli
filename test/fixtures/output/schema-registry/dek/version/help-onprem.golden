Manage Schema Registry DEK versions.

Usage:
  confluent schema-registry dek version [command]

Available Commands:
  list        List Schema Registry DEK versions.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent schema-registry dek version [command] --help" for more information about a command.
