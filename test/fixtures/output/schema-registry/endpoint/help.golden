Manage the Schema Registry endpoints for the current environment.

Usage:
  confluent schema-registry endpoint [command]

Available Commands:
  list        List all schema registry endpoints.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent schema-registry endpoint [command] --help" for more information about a command.
