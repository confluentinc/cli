Manage Schema Registry.

Usage:
  cflt schema-registry [command]

Available Commands:
  cluster     Manage Schema Registry cluster.
  schema      Manage Schema Registry schemas.
  subject     Manage Schema Registry subjects.

Global Flags:
  -h, --help            Show help for this command.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "cflt schema-registry [command] --help" for more information about a command.
