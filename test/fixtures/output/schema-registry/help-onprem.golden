Manage Schema Registry.

Usage:
  confluent schema-registry [command]

Aliases:
  schema-registry, sr

Available Commands:
  cluster       Manage Schema Registry clusters.
  compatibility Validate schema compatibility.
  config        Manage Schema Registry configuration.
  exporter      Manage Schema Registry exporters.
  schema        Manage Schema Registry schemas.
  subject       Manage Schema Registry subjects.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which may contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent schema-registry [command] --help" for more information about a command.
