Manage private link egress endpoints.

Usage:
  confluent network access-point private-link egress-endpoint [command]

Available Commands:
  create      Create an egress endpoint.
  delete      Delete one or more egress endpoints.
  describe    Describe an egress endpoint.
  list        List egress endpoints.
  update      Update an existing egress endpoint.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent network access-point private-link egress-endpoint [command] --help" for more information about a command.
