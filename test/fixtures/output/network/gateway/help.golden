Manage network gateways.

Usage:
  confluent network gateway [command]

Available Commands:
  create      Create a network gateway.
  delete      Delete one or more gateways.
  describe    Describe a gateway.
  list        List gateways.
  update      Update a gateway.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent network gateway [command] --help" for more information about a command.
