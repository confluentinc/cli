Manage network link service associations.

Usage:
  confluent network network-link service association [command]

Available Commands:
  describe    Describe a network link service association.
  list        List associations for a network link service.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent network network-link service association [command] --help" for more information about a command.
