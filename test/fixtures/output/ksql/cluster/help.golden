Manage ksqlDB clusters.

Usage:
  confluent ksql cluster [command]

Available Commands:
  configure-acls Configure ACLs for a ksqlDB cluster.
  create         Create a ksqlDB cluster.
  delete         Delete ksqlDB clusters.
  describe       Describe a ksqlDB cluster.
  list           List ksqlDB clusters.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent ksql cluster [command] --help" for more information about a command.
