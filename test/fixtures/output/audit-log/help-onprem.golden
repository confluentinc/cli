Manage which auditable events are logged, and where the event logs are sent.

Usage:
  confluent audit-log [command]

Aliases:
  audit-log, al

Available Commands:
  config      Manage the audit log configuration specification.
  route       Return the audit log route rules.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which may contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent audit-log [command] --help" for more information about a command.
