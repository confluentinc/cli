Manage provider shares.

Usage:
  confluent stream-share provider share [command]

Available Commands:
  delete      Delete provider shares.
  describe    Describe a provider share.
  list        List shares for provider.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent stream-share provider share [command] --help" for more information about a command.
