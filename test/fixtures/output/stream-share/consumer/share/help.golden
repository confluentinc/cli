Manage consumer shares.

Usage:
  confluent stream-share consumer share [command]

Available Commands:
  delete      Delete consumer shares.
  describe    Describe a consumer share.
  list        List consumer shares.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which may contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent stream-share consumer share [command] --help" for more information about a command.
