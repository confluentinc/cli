Manage Confluent Cloud organizations.

Usage:
  confluent organization [command]

Aliases:
  organization, org

Available Commands:
  describe    Describe the current organization.
  list        List organizations.
  update      Update the current organization.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent organization [command] --help" for more information about a command.
