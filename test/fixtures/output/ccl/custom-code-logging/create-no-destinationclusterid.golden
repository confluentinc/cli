Error: if any flags in the group [destination-kafka destination-topic destination-cluster-id] are set they must all be set; missing [destination-cluster-id]
Usage:
  confluent ccl custom-code-logging create [flags]

Examples:
Create custom code logging.

  $ confluent ccl custom-code-logging create --cloud aws --region us-west-2 --environment env-000000 --destination-kafka --destination-topic topic-123 --destination-cluster-id cluster-123

Flags:
      --cloud string                    REQUIRED: Specify the cloud provider as "aws", "azure", or "gcp".
      --region string                   REQUIRED: Cloud region for Kafka (use "confluent kafka region list" to see all).
      --environment string              REQUIRED: Environment ID.
      --destination-kafka               Set custom code logging destination to KAFKA (default true)
      --destination-topic string        Kafka topic of custom code logging destination.
      --destination-cluster-id string   Kafka cluster id of custom code logging destination.
      --log-level string                Log level of custom code logging. (default "INFO")
      --context string                  CLI context name.
  -o, --output string                   Specify the output format as "human", "json", or "yaml". (default "human")

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

