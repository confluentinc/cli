Manage Schema Registry.

Usage:
  ccloud schema-registry [command]

Available Commands:
  cluster     Manage Schema Registry cluster.
  schema      Manage Schema Registry schemas.
  subject     List subjects.

Flags:
  -h, --help   help for cluster

Global Flags:
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "ccloud schema-registry [command] --help" for more information about a command.
