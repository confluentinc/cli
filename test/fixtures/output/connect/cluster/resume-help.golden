Resume connectors.

Usage:
  confluent connect cluster resume <id|name-1> [id|name-2] ... [id|name-N] [flags]

Examples:
Resume connectors "lcc-000001" and "lcc-000002":

  $ confluent connect cluster resume lcc-000001 lcc-000002

Flags:
      --cluster string       Kafka cluster ID or name.
      --context string       CLI context name.
      --environment string   Confluent Cloud environment ID or name.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).
