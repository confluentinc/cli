Error: accepts 1 arg(s), received 0
Usage:
  confluent kafka consumer-group lag get <consumer-group> [flags]

Examples:
Get the consumer lag for topic ``my_topic`` partition ``0`` consumed by consumer-group ``my_consumer_group``.

::

  ccloud kafka consumer-group lag get my_consumer_group --topic my_topic --partition 0



Flags:
  -o, --output string        Specify the output format as "human", "json", or "yaml". (default "human")
      --topic string         Topic name.
      --partition int32      Partition ID.
      --environment string   Environment ID.
      --cluster string       Kafka cluster ID.
      --context string       CLI Context name.

Global Flags:
  -h, --help            Show help for this command.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

