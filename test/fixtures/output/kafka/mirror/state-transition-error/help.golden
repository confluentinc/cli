Manages state transition errors.

Usage:
  confluent kafka mirror state-transition-error [command]

Available Commands:
  list        Lists the mirror topic's state transition errors.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent kafka mirror state-transition-error [command] --help" for more information about a command.
