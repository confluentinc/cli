Manage Kafka replicas.

Usage:
  confluent kafka replica [command]

Available Commands:
  list        List Kafka replica statuses.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but plaintext secrets may be printed in HTTP requests/responses.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent kafka replica [command] --help" for more information about a command.
