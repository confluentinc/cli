Manage Kafka streams group member assignment subtopologies.

Usage:
  confluent kafka streams-group member assignment subtopology [command]

Available Commands:
  describe    Describe a Kafka streams group member task partitions.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent kafka streams-group member assignment subtopology [command] --help" for more information about a command.
