Manage stream group target task partitions.

Usage:
  confluent kafka streams-group member-target-assignment-task-partitions [command]

Available Commands:
  describe    Describe stream group target task partitions.

Global Flags:
  -h, --help            Show help for this command.
      --unsafe-trace    Equivalent to -vvvv, but also log HTTP requests and responses which might contain plaintext secrets.
  -v, --verbose count   Increase verbosity (-v for warn, -vv for info, -vvv for debug, -vvvv for trace).

Use "confluent kafka streams-group member-target-assignment-task-partitions [command] --help" for more information about a command.
