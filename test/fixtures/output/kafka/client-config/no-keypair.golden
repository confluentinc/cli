Error: no API key selected for resource "lkc-cool1"

Suggestions:
    Select an API key for resource "lkc-cool1" with `confluent api-key use <API_KEY>`.
    To do so, you must have either already created or stored an API key for the resource.
    To create an API key, use `confluent api-key create --resource lkc-cool1`.
    To store an existing API key, use `confluent api-key store --resource lkc-cool1`.
