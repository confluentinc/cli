{
  "name": "test-connector",
  "config": {
    "name": "test-connector",
    "data.format": "JSON",
    "kafka.api.key": "key",
    "kafka.api.secret": "key",
    "tasks.max": "1",
    "time.interval": "HOURLY",
    "topics": "test",
    "connector.class": "DataGenConnector"
  }
}
