// Code generated by cli-terraform-generator v0.11.2; DO NOT EDIT.

package ccpm

import (
	"strings"

	"github.com/spf13/cobra"

	pcmd "github.com/confluentinc/cli/v4/pkg/cmd"
	"github.com/confluentinc/cli/v4/pkg/examples"
	"github.com/confluentinc/cli/v4/pkg/output"
)

func (c *customConnectPluginCommand) newListCommand() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "list",
		Short: "List CCPM custom Connect plugins.",
		Args:  cobra.NoArgs,
		RunE:  c.list,
		Example: examples.BuildExampleString(
			examples.Example{
				Text: "List all custom Connect plugins in an environment.",
				Code: "confluent ccpm plugin list --environment env-12345",
			},
			examples.Example{
				Text: "List custom Connect plugins filtered by cloud provider.",
				Code: "confluent ccpm plugin list --environment env-12345 --cloud AWS",
			},
		),
	}

	// Required flags

	// Optional flags
	pcmd.AddCloudFlag(cmd)
	pcmd.AddEnvironmentFlag(cmd, c.AuthenticatedCLICommand)

	pcmd.AddContextFlag(cmd, c.CLICommand)
	pcmd.AddOutputFlag(cmd)

	return cmd
}

func (c *customConnectPluginCommand) list(cmd *cobra.Command, _ []string) error {
	cloud, err := cmd.Flags().GetString("cloud")
	if err != nil {
		return err
	}
	cloud = strings.ToUpper(cloud)

	environmentId, err := c.Context.EnvironmentId()
	if err != nil {
		return err
	}

	customConnectPlugins, err := c.V2Client.ListCcpmCustomConnectPlugins(cloud, environmentId)
	if err != nil {
		return err
	}

	list := output.NewList(cmd)
	for _, customConnectPlugin := range customConnectPlugins {
		out := &customConnectPluginOut{
			ID:              customConnectPlugin.GetId(),
			Cloud:           customConnectPlugin.Spec.GetCloud(),
			Name:            customConnectPlugin.Spec.GetDisplayName(),
			Environment:     customConnectPlugin.Spec.Environment.GetId(),
			RuntimeLanguage: customConnectPlugin.Spec.GetRuntimeLanguage(),
			Description:     customConnectPlugin.Spec.GetDescription(),
		}
		list.Add(out)
	}
	return list.Print()
}
