// Code generated by cli-terraform-generator v0.8.4; DO NOT EDIT.

package ccpm

import (
	"github.com/spf13/cobra"

	pcmd "github.com/confluentinc/cli/v4/pkg/cmd"
	"github.com/confluentinc/cli/v4/pkg/errors"
	"github.com/confluentinc/cli/v4/pkg/examples"
)

func (c *customConnectPluginCommand) newDescribeCommand() *cobra.Command {
	cmd := &cobra.Command{
		Use:               "describe <id>",
		Short:             "Describe a CCPM custom Connect plugin.",
		Args:              cobra.ExactArgs(1),
		ValidArgsFunction: pcmd.NewValidArgsFunction(c.validArgs),
		RunE:              c.describe,
		Example: examples.BuildExampleString(
			examples.Example{
				Text: "Describe a custom Connect plugin by ID.",
				Code: "confluent ccpm custom-connect-plugin describe ccp-123456 --environment env-12345",
			},
		),
	}

	// Required flags

	// Optional flags
	pcmd.AddEnvironmentFlag(cmd, c.AuthenticatedCLICommand)

	pcmd.AddContextFlag(cmd, c.CLICommand)
	pcmd.AddOutputFlag(cmd)

	return cmd
}

func (c *customConnectPluginCommand) describe(cmd *cobra.Command, args []string) error {
	id := args[0]
	environmentId, err := c.Context.EnvironmentId()
	if err != nil {
		return err
	}

	customConnectPlugin, httpResp, err := c.V2Client.GetCcpmCustomConnectPlugin(id, environmentId)
	if err != nil {
		return errors.CatchCCloudV2Error(err, httpResp)
	}

	return printCustomConnectPlugin(cmd, customConnectPlugin)
}
