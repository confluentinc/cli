// Code generated by cli-terraform-generator v0.8.7; DO NOT EDIT.

package ccpm

import (
	"github.com/spf13/cobra"

	ccpmv1 "github.com/confluentinc/ccloud-sdk-go-v2/ccpm/v1"

	pcmd "github.com/confluentinc/cli/v4/pkg/cmd"
	"github.com/confluentinc/cli/v4/pkg/errors"
	"github.com/confluentinc/cli/v4/pkg/examples"
	"github.com/confluentinc/cli/v4/pkg/output"
)

func (c *customConnectPluginCommand) newUpdateCommand() *cobra.Command {
	cmd := &cobra.Command{
		Use:               "update <id>",
		Short:             "Update an existing CCPM custom Connect plugin.",
		Args:              cobra.ExactArgs(1),
		ValidArgsFunction: pcmd.NewValidArgsFunction(c.validArgs),
		RunE:              c.update,
		Example: examples.BuildExampleString(
			examples.Example{
				Text: "Update the name and description of a custom Connect plugin.",
				Code: `confluent ccpm plugin update ccp-123456 --name "Updated Plugin Name" --description "Updated description" --environment env-12345`,
			},
			examples.Example{
				Text: "Update only the name of a custom Connect plugin.",
				Code: `confluent ccpm plugin update ccp-123456 --name "New Plugin Name" --environment env-12345`,
			},
		),
	}

	// Required flags

	// Optional flags
	cmd.Flags().String("description", "", "Description of Custom Connect Plugin.")
	cmd.Flags().String("name", "", "Display name of Custom Connect Plugin.")
	pcmd.AddEnvironmentFlag(cmd, c.AuthenticatedCLICommand)

	pcmd.AddContextFlag(cmd, c.CLICommand)
	pcmd.AddOutputFlag(cmd)

	return cmd
}

func (c *customConnectPluginCommand) update(cmd *cobra.Command, args []string) error {
	id := args[0]

	updateReq := ccpmv1.CcpmV1CustomConnectPluginUpdate{}
	specUpdate := ccpmv1.CcpmV1CustomConnectPluginSpecUpdate{}

	if cmd.Flags().Changed("description") {
		description, err := cmd.Flags().GetString("description")
		if err != nil {
			return err
		}
		specUpdate.Description = ccpmv1.PtrString(description)
	}

	if cmd.Flags().Changed("name") {
		displayName, err := cmd.Flags().GetString("name")
		if err != nil {
			return err
		}
		specUpdate.DisplayName = ccpmv1.PtrString(displayName)
	}

	environmentId, err := c.Context.EnvironmentId()
	if err != nil {
		return err
	}
	if environmentId != "" {
		specUpdate.Environment = &ccpmv1.EnvScopedObjectReference{Id: environmentId}
	}

	updateReq.Spec = &specUpdate
	customConnectPlugin, httpResp, err := c.V2Client.UpdateCcpmCustomConnectPlugin(id, updateReq)
	if err != nil {
		return errors.CatchCCloudV2Error(err, httpResp)
	}

	if output.GetFormat(cmd) == output.Human {
		output.Printf(c.Config.EnableColor, "Updated CCPM custom Connect plugin \"%s\".\n", id)
	}
	return printCustomConnectPlugin(cmd, customConnectPlugin)
}
