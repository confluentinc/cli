// Code generated by cli-terraform-generator v0.22.1; DO NOT EDIT.

package organization

import (
	"github.com/spf13/cobra"

	pcmd "github.com/confluentinc/cli/v4/pkg/cmd"
	"github.com/confluentinc/cli/v4/pkg/errors"
)

func (c *organizationCommand) newDescribeCommand() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "describe",
		Short: "Describe the current organization.",
		Args:  cobra.NoArgs,
		RunE:  c.describe,
	}

	// Required flags

	// Optional flags

	pcmd.AddContextFlag(cmd, c.CLICommand)
	pcmd.AddOutputFlag(cmd)

	return cmd
}

func (c *organizationCommand) describe(cmd *cobra.Command, _ []string) error {
	id := c.Context.GetCurrentOrganization()

	organization, err := c.V2Client.GetOrgOrganization(id)
	if err != nil {
		return errors.NewErrorWithSuggestions(
			err.Error(),
			"List available organizations with `confluent organization list`.",
		)
	}

	return c.printOrganization(cmd, organization)
}
