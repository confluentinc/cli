// Code generated by cli-terraform-generator; DO NOT EDIT.

package endpoint

import (
	"strings"

	"github.com/spf13/cobra"

	pcmd "github.com/confluentinc/cli/v4/pkg/cmd"
	"github.com/confluentinc/cli/v4/pkg/output"
)

func (c *endpointCommand) newListCommand() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "list",
		Short: "List endpoints.",
		Args:  cobra.NoArgs,
		RunE:  c.list,
	}

	// Required flags
	cmd.Flags().String("service", "", "Filter the results by exact match for service.")
	cobra.CheckErr(cmd.MarkFlagRequired("service"))

	// Optional flags
	pcmd.AddCloudFlag(cmd)
	cmd.Flags().String("region", "", "Filter the results by exact match for region.")
	cmd.Flags().Bool("is-private", false, "Filter the results by whether the endpoint is private (true) or public (false). If not specified, returns both private and public endpoints.")
	pcmd.AddEnvironmentFlag(cmd, c.AuthenticatedCLICommand)
	cmd.Flags().String("resource", "", "Filter the results by exact match for resource.")

	pcmd.AddContextFlag(cmd, c.CLICommand)
	pcmd.AddOutputFlag(cmd)

	return cmd
}

func (c *endpointCommand) list(cmd *cobra.Command, _ []string) error {
	cloud, err := cmd.Flags().GetString("cloud")
	if err != nil {
		return err
	}
	cloud = strings.ToUpper(cloud)

	region, err := cmd.Flags().GetString("region")
	if err != nil {
		return err
	}

	service, err := cmd.Flags().GetString("service")
	if err != nil {
		return err
	}

	var isPrivate *bool
	if cmd.Flags().Changed("is-private") {
		v, err := cmd.Flags().GetBool("is-private")
		if err != nil {
			return err
		}
		isPrivate = &v
	}

	environmentId, err := c.Context.EnvironmentId()
	if err != nil {
		return err
	}

	resource, err := cmd.Flags().GetString("resource")
	if err != nil {
		return err
	}

	endpoints, err := c.V2Client.ListEndpoints(cloud, region, service, isPrivate, environmentId, resource)
	if err != nil {
		return err
	}

	list := output.NewList(cmd)
	for _, endpoint := range endpoints {
		out := &endpointOut{
			Cloud:          endpoint.GetCloud(),
			Region:         endpoint.GetRegion(),
			Service:        endpoint.GetService(),
			IsPrivate:      endpoint.GetIsPrivate(),
			ConnectionType: endpoint.GetConnectionType(),
			Endpoint:       endpoint.GetEndpoint(),
			EndpointType:   endpoint.GetEndpointType(),
			Environment:    endpoint.Environment.GetId(),
			Resource:       endpoint.Resource.GetId(),
			Gateway:        endpoint.Gateway.GetId(),
			AccessPoint:    endpoint.AccessPoint.GetId(),
		}
		list.Add(out)
	}
	return list.Print()
}
