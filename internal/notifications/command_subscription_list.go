// Code generated by cli-terraform-generator v0.8.4; DO NOT EDIT.

package notifications

import (
	"github.com/spf13/cobra"

	pcmd "github.com/confluentinc/cli/v4/pkg/cmd"
	"github.com/confluentinc/cli/v4/pkg/output"
)

func (c *subscriptionCommand) newListCommand() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "list",
		Short: "List notifications subscriptions.",
		Args:  cobra.NoArgs,
		RunE:  c.list,
	}

	// Required flags

	// Optional flags

	pcmd.AddContextFlag(cmd, c.CLICommand)
	pcmd.AddOutputFlag(cmd)

	return cmd
}

func (c *subscriptionCommand) list(cmd *cobra.Command, _ []string) error {
	subscriptions, err := c.V2Client.ListNotificationsSubscriptions()
	if err != nil {
		return err
	}

	list := output.NewList(cmd)
	for _, subscription := range subscriptions {
		out := &subscriptionOut{
			ID:               subscription.GetId(),
			NotificationType: subscription.NotificationType.GetId(),
			CurrentState:     subscription.GetCurrentState(),
		}
		list.Add(out)
	}
	return list.Print()
}
