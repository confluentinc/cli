// Code generated by cli-terraform-generator; DO NOT EDIT.

package notifications

import (
	"github.com/spf13/cobra"

	pcmd "github.com/confluentinc/cli/v4/pkg/cmd"
	"github.com/confluentinc/cli/v4/pkg/output"
)

func (c *notificationTypeCommand) newListCommand() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "list",
		Short: "List notifications notification types.",
		Args:  cobra.NoArgs,
		RunE:  c.list,
	}

	// Required flags

	// Optional flags
	cmd.Flags().String("resource-type", "", "Confluent Cloud resource type")

	pcmd.AddContextFlag(cmd, c.CLICommand)
	pcmd.AddOutputFlag(cmd)

	return cmd
}

func (c *notificationTypeCommand) list(cmd *cobra.Command, _ []string) error {
	resourceType, err := cmd.Flags().GetString("resource-type")
	if err != nil {
		return err
	}

	notificationTypes, err := c.V2Client.ListNotificationsNotificationTypes(resourceType)
	if err != nil {
		return err
	}

	list := output.NewList(cmd)
	for _, notificationType := range notificationTypes {
		out := &notificationTypeOut{
			ID:                   notificationType.GetId(),
			DisplayName:          notificationType.GetDisplayName(),
			Category:             notificationType.GetCategory(),
			Description:          notificationType.GetDescription(),
			SubscriptionPriority: notificationType.GetSubscriptionPriority(),
			IsIncludedInPlan:     notificationType.GetIsIncludedInPlan(),
			Severity:             notificationType.GetSeverity(),
		}
		list.Add(out)
	}
	return list.Print()
}
