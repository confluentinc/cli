// Code generated by cli-terraform-generator; DO NOT EDIT.

package notifications

import (
	"time"

	"github.com/spf13/cobra"

	pcmd "github.com/confluentinc/cli/v4/pkg/cmd"
	"github.com/confluentinc/cli/v4/pkg/output"
)

func (c *userNotificationCommand) newListCommand() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "list",
		Short: "List notifications user notifications.",
		Args:  cobra.NoArgs,
		RunE:  c.list,
	}

	// Required flags

	// Optional flags
	cmd.Flags().Bool("read", false, "Filter the results where read is true or false.")
	cmd.Flags().StringSlice("severity", nil, "A comma-separated list of severity levels to filter by.")
	cmd.Flags().String("include", "", "Comma-separated list of optional fields to populate in the response items. Allowed values: `integrations`, `recommended_actions`. By default these fields are omitted from list responses to keep collection payloads slim; set this parameter to opt in. This is a partial-response selector, not a value filter.")
	cmd.Flags().StringSlice("type", nil, "A comma-separated list of Confluent Cloud resource types to filter by.")
	cmd.Flags().StringSlice("crn", nil, "A comma-separated list of Confluent Cloud resource CRNs to filter by.")
	cmd.Flags().String("search", "", "Free-text partial-match search across the embedded notification type's `display_name` and `description`.")
	cmd.Flags().String("time-range", "", "Filter notifications by a preset time window relative to now. Allowed values: `PAST_24H` (last 24 hours), `PAST_7D` (last 7 days), `PAST_30D` (last 30 days).")
	cmd.Flags().StringSlice("sort", nil, "A comma-separated list of fields and directions to sort by.")

	pcmd.AddContextFlag(cmd, c.CLICommand)
	pcmd.AddOutputFlag(cmd)

	return cmd
}

func (c *userNotificationCommand) list(cmd *cobra.Command, _ []string) error {
	var read *bool
	if cmd.Flags().Changed("read") {
		v, err := cmd.Flags().GetBool("read")
		if err != nil {
			return err
		}
		read = &v
	}

	severity, err := cmd.Flags().GetStringSlice("severity")
	if err != nil {
		return err
	}

	include, err := cmd.Flags().GetString("include")
	if err != nil {
		return err
	}

	typeValue, err := cmd.Flags().GetStringSlice("type")
	if err != nil {
		return err
	}

	crn, err := cmd.Flags().GetStringSlice("crn")
	if err != nil {
		return err
	}

	search, err := cmd.Flags().GetString("search")
	if err != nil {
		return err
	}

	timeRange, err := cmd.Flags().GetString("time-range")
	if err != nil {
		return err
	}

	sort, err := cmd.Flags().GetStringSlice("sort")
	if err != nil {
		return err
	}

	userNotifications, err := c.V2Client.ListNotificationsUserNotifications(read, severity, include, typeValue, crn, search, timeRange, sort)
	if err != nil {
		return err
	}

	list := output.NewList(cmd)
	for _, userNotification := range userNotifications {
		out := &userNotificationOut{
			ID:         userNotification.GetId(),
			ReadAt:     userNotification.GetReadAt().Format(time.RFC3339),
			Severity:   userNotification.GetSeverity(),
			ReceivedAt: userNotification.GetReceivedAt().Format(time.RFC3339),
			Read:       userNotification.GetRead(),
		}
		list.Add(out)
	}
	return list.Print()
}
