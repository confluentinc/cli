// Code generated by cli-terraform-generator v0.25.3; DO NOT EDIT.

package iam

import (
	"fmt"
	"strings"

	"github.com/spf13/cobra"

	ssov2 "github.com/confluentinc/ccloud-sdk-go-v2/sso/v2"

	pcmd "github.com/confluentinc/cli/v4/pkg/cmd"
	"github.com/confluentinc/cli/v4/pkg/examples"
	"github.com/confluentinc/cli/v4/pkg/output"
)

func (c *groupMappingCommand) newUpdateCommand() *cobra.Command {
	cmd := &cobra.Command{
		Use:               "update <id>",
		Short:             "Update an existing IAM group mapping.",
		Args:              cobra.ExactArgs(1),
		ValidArgsFunction: pcmd.NewValidArgsFunction(c.validArgs),
		RunE:              c.update,
		Example: examples.BuildExampleString(
			examples.Example{
				Text: `Update the description of group mapping "group-123456".`,
				Code: `confluent iam group-mapping update group-123456 --description "updated description"`,
			},
		),
	}

	// Required flags

	// Optional flags
	cmd.Flags().String("name", "", "Name of the group mapping.")
	cmd.Flags().String("description", "", "Description of the group mapping.")
	cmd.Flags().String("filter", "true", "A supported Common Expression Language (CEL) filter expression.")

	pcmd.AddContextFlag(cmd, c.CLICommand)
	pcmd.AddOutputFlag(cmd)

	return cmd
}

func (c *groupMappingCommand) update(cmd *cobra.Command, args []string) error {
	id := args[0]
	if !strings.HasPrefix(id, "group-") {
		return fmt.Errorf("failed parsing resource ID: missing prefix \"group-\" is required")
	}

	updateReq := ssov2.IamV2SsoGroupMapping{}

	if cmd.Flags().Changed("name") {
		displayName, err := cmd.Flags().GetString("name")
		if err != nil {
			return err
		}
		updateReq.DisplayName = ssov2.PtrString(displayName)
	}

	if cmd.Flags().Changed("description") {
		description, err := cmd.Flags().GetString("description")
		if err != nil {
			return err
		}
		updateReq.Description = ssov2.PtrString(description)
	}

	if cmd.Flags().Changed("filter") {
		filter, err := cmd.Flags().GetString("filter")
		if err != nil {
			return err
		}
		updateReq.Filter = ssov2.PtrString(filter)
	}

	groupMapping, err := c.V2Client.UpdateIamGroupMapping(id, updateReq)
	if err != nil {
		return err
	}

	if output.GetFormat(cmd) == output.Human {
		output.Printf(c.Config.EnableColor, "Updated IAM group mapping \"%s\".\n", id)
	}
	return printGroupMapping(cmd, groupMapping)
}
