// Code generated by cli-terraform-generator v0.12.1; DO NOT EDIT.

package iam

import (
	"fmt"

	"github.com/spf13/cobra"

	iamv2 "github.com/confluentinc/ccloud-sdk-go-v2/iam/v2"

	pcmd "github.com/confluentinc/cli/v4/pkg/cmd"
	"github.com/confluentinc/cli/v4/pkg/config"
	"github.com/confluentinc/cli/v4/pkg/output"
)

type userCommand struct {
	*pcmd.AuthenticatedCLICommand
}

// authMethodFormats maps the iam.v2 AUTH_TYPE_* enum values the API returns to the
// human-friendly labels the CLI has always displayed. Hand-written: the OpenAPI spec
// only declares the raw enum, so the generator cannot produce this mapping.
// TODO: discuss with Identity Team on the response and what to expect for CLI v5
var authMethodFormats = map[string]string{
	"AUTH_TYPE_LOCAL":   "Username/Password",
	"AUTH_TYPE_SSO":     "SSO",
	"AUTH_TYPE_UNKNOWN": "Unknown",
}

type userOut struct {
	ID                   string `human:"ID" serialized:"id"`
	Name                 string `human:"Name" serialized:"name"`
	AuthenticationMethod string `human:"Authentication Method" serialized:"authentication_method"`
	Email                string `human:"Email" serialized:"email"`
}

func newUserCommand(cfg *config.Config, prerunner pcmd.PreRunner) *cobra.Command { //nolint:unparam
	cmd := &cobra.Command{
		Use:         "user",
		Short:       "Manage Confluent Cloud IAM users.",
		Annotations: map[string]string{pcmd.RunRequirement: pcmd.RequireNonAPIKeyCloudLogin},
	}

	c := &userCommand{
		AuthenticatedCLICommand: pcmd.NewAuthenticatedCLICommand(cmd, prerunner),
	}

	cmd.AddCommand(
		c.newDeleteCommand(),
		c.newDescribeCommand(),
		c.newListCommand(),
		c.newUpdateCommand(),
		newInvitationCommand(prerunner),
		// cli-tfgen:cli-subcommands
	)

	return cmd
}

func printUser(cmd *cobra.Command, user iamv2.IamV2User) error {
	table := output.NewTable(cmd)
	out := &userOut{
		ID:                   user.GetId(),
		Name:                 user.GetFullName(),
		AuthenticationMethod: authMethodFormats[user.GetAuthType()],
		Email:                user.GetEmail(),
	}
	table.Add(out)
	return table.Print()
}

func (c *userCommand) validArgs(cmd *cobra.Command, args []string) []string {
	if len(args) > 0 {
		return nil
	}

	return c.validArgsMultiple(cmd, args)
}

func (c *userCommand) validArgsMultiple(cmd *cobra.Command, args []string) []string {
	if err := c.PersistentPreRunE(cmd, args); err != nil {
		return nil
	}

	return c.autocompleteUsers()
}

func (c *userCommand) autocompleteUsers() []string {
	users, err := c.V2Client.ListIamUsers()
	if err != nil {
		return nil
	}

	suggestions := make([]string, len(users))
	for i, user := range users {
		suggestions[i] = fmt.Sprintf("%s\t%s", user.GetId(), user.GetFullName())
	}
	return suggestions
}
