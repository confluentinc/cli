// Code generated by cli-terraform-generator v0.16.1; DO NOT EDIT.

package iam

import (
	"fmt"
	"strings"

	"github.com/spf13/cobra"

	identityproviderv2 "github.com/confluentinc/ccloud-sdk-go-v2/identity-provider/v2"

	pcmd "github.com/confluentinc/cli/v4/pkg/cmd"
	"github.com/confluentinc/cli/v4/pkg/examples"
	"github.com/confluentinc/cli/v4/pkg/output"
)

func (c *identityProviderCommand) newUpdateCommand() *cobra.Command {
	cmd := &cobra.Command{
		Use:               "update <id>",
		Short:             "Update an existing IAM identity provider.",
		Args:              cobra.ExactArgs(1),
		ValidArgsFunction: pcmd.NewValidArgsFunction(c.validArgs),
		RunE:              c.update,
		Example: examples.BuildExampleString(
			examples.Example{
				Text: `Update the description of identity provider "op-123456".`,
				Code: `confluent iam identity-provider update op-123456 --description "updated description"`,
			},
		),
	}

	// Required flags

	// Optional flags
	cmd.Flags().String("name", "", "The human-readable name of the OAuth identity provider.")
	cmd.Flags().String("description", "", "A description of the identity provider.")
	cmd.Flags().String("identity-claim", "", "The JSON Web Token (JWT) claim to extract the authenticating identity to Confluent resources from Registered Claim Names.")
	cmd.Flags().String("issuer-uri", "", "A publicly accessible URL uniquely identifying the OAuth identity provider authorized to issue access tokens.")
	cmd.Flags().String("jwks-uri", "", "A publicly accessible JSON Web Key Set (JWKS) URI for the OAuth identity provider. JWKS provides a set of cryptographic keys used to verify the authenticity and integrity of JSON Web Tokens (JWTs) issued by the OAuth identity provider.")

	pcmd.AddContextFlag(cmd, c.CLICommand)
	pcmd.AddOutputFlag(cmd)

	return cmd
}

func (c *identityProviderCommand) update(cmd *cobra.Command, args []string) error {
	id := args[0]
	if !strings.HasPrefix(id, "op-") {
		return fmt.Errorf("failed parsing resource ID: missing prefix \"op-\" is required")
	}

	updateReq := identityproviderv2.IamV2IdentityProvider{}

	if cmd.Flags().Changed("name") {
		displayName, err := cmd.Flags().GetString("name")
		if err != nil {
			return err
		}
		updateReq.DisplayName = identityproviderv2.PtrString(displayName)
	}

	if cmd.Flags().Changed("description") {
		description, err := cmd.Flags().GetString("description")
		if err != nil {
			return err
		}
		updateReq.Description = identityproviderv2.PtrString(description)
	}

	if cmd.Flags().Changed("identity-claim") {
		identityClaim, err := cmd.Flags().GetString("identity-claim")
		if err != nil {
			return err
		}
		updateReq.IdentityClaim = identityproviderv2.PtrString(identityClaim)
	}

	if cmd.Flags().Changed("issuer-uri") {
		issuer, err := cmd.Flags().GetString("issuer-uri")
		if err != nil {
			return err
		}
		updateReq.Issuer = identityproviderv2.PtrString(issuer)
	}

	if cmd.Flags().Changed("jwks-uri") {
		jwksUri, err := cmd.Flags().GetString("jwks-uri")
		if err != nil {
			return err
		}
		updateReq.JwksUri = identityproviderv2.PtrString(jwksUri)
	}

	updateReq.Id = identityproviderv2.PtrString(id)
	identityProvider, err := c.V2Client.UpdateIamIdentityProvider(updateReq)
	if err != nil {
		return err
	}

	if output.GetFormat(cmd) == output.Human {
		output.Printf(c.Config.EnableColor, "Updated IAM identity provider \"%s\".\n", id)
	}
	return printIdentityProvider(cmd, identityProvider)
}
