// Code generated by cli-terraform-generator v0.16.1; DO NOT EDIT.

package iam

import (
	"fmt"
	"strings"

	"github.com/spf13/cobra"

	pcmd "github.com/confluentinc/cli/v4/pkg/cmd"
)

func (c *identityProviderCommand) newDescribeCommand() *cobra.Command {
	cmd := &cobra.Command{
		Use:               "describe <id>",
		Short:             "Describe an IAM identity provider.",
		Args:              cobra.ExactArgs(1),
		ValidArgsFunction: pcmd.NewValidArgsFunction(c.validArgs),
		RunE:              c.describe,
	}

	// Required flags

	// Optional flags

	pcmd.AddContextFlag(cmd, c.CLICommand)
	pcmd.AddOutputFlag(cmd)

	return cmd
}

func (c *identityProviderCommand) describe(cmd *cobra.Command, args []string) error {
	id := args[0]
	if !strings.HasPrefix(id, "op-") {
		return fmt.Errorf("failed parsing resource ID: missing prefix \"op-\" is required")
	}

	identityProvider, err := c.V2Client.GetIamIdentityProvider(id)
	if err != nil {
		return err
	}

	return printIdentityProvider(cmd, identityProvider)
}
