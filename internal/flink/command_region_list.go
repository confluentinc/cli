// Code generated by cli-terraform-generator; DO NOT EDIT.

package flink

import (
	"strings"

	"github.com/spf13/cobra"

	pcmd "github.com/confluentinc/cli/v4/pkg/cmd"
	"github.com/confluentinc/cli/v4/pkg/examples"
	"github.com/confluentinc/cli/v4/pkg/output"
)

func (c *regionCommand) newListCommand() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "list",
		Short: "List Flink regions.",
		Args:  cobra.NoArgs,
		RunE:  c.list,
		Example: examples.BuildExampleString(
			examples.Example{
				Text: "List the available Flink AWS regions.",
				Code: "confluent flink region list --cloud aws",
			},
		),
	}

	// Required flags

	// Optional flags
	pcmd.AddCloudFlag(cmd)
	cmd.Flags().String("region", "", "Filter the results by exact match for region_name.")

	pcmd.AddContextFlag(cmd, c.CLICommand)
	pcmd.AddOutputFlag(cmd)

	return cmd
}

func (c *regionCommand) list(cmd *cobra.Command, _ []string) error {
	cloud, err := cmd.Flags().GetString("cloud")
	if err != nil {
		return err
	}
	cloud = strings.ToUpper(cloud)

	regionName, err := cmd.Flags().GetString("region")
	if err != nil {
		return err
	}

	regions, err := c.V2Client.ListFlinkRegions(cloud, regionName)
	if err != nil {
		return err
	}

	list := output.NewList(cmd)
	for _, region := range regions {
		out := &regionOut{
			IsCurrent: region.GetCloud() == strings.ToUpper(c.Context.GetCurrentFlinkCloudProvider()) && region.GetRegionName() == c.Context.GetCurrentFlinkRegion(),
			Name:      region.GetDisplayName(),
			Cloud:     region.GetCloud(),
			Region:    region.GetRegionName(),
		}
		list.Add(out)
	}
	return list.Print()
}
