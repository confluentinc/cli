// Code generated by cli-terraform-generator v0.8.7; DO NOT EDIT.

package flink

import (
	"github.com/spf13/cobra"

	pcmd "github.com/confluentinc/cli/v4/pkg/cmd"
	"github.com/confluentinc/cli/v4/pkg/output"
)

func (c *computePoolCommand) newListCommand() *cobra.Command {
	cmd := &cobra.Command{
		Use:   "list",
		Short: "List Flink compute pools.",
		Args:  cobra.NoArgs,
		RunE:  c.list,
	}

	// Required flags

	// Optional flags
	pcmd.AddRegionFlagFlink(cmd, c.AuthenticatedCLICommand)
	pcmd.AddEnvironmentFlag(cmd, c.AuthenticatedCLICommand)

	pcmd.AddContextFlag(cmd, c.CLICommand)
	pcmd.AddOutputFlag(cmd)

	return cmd
}

func (c *computePoolCommand) list(cmd *cobra.Command, _ []string) error {
	region, err := cmd.Flags().GetString("region")
	if err != nil {
		return err
	}

	environmentId, err := c.Context.EnvironmentId()
	if err != nil {
		return err
	}

	computePools, err := c.V2Client.ListFlinkComputePools(region, environmentId, "")
	if err != nil {
		return err
	}

	list := output.NewList(cmd)
	for _, computePool := range computePools {
		out := &computePoolOut{
			IsCurrent:   computePool.GetId() == c.Context.GetCurrentFlinkComputePool(),
			ID:          computePool.GetId(),
			Name:        computePool.Spec.GetDisplayName(),
			Cloud:       computePool.Spec.GetCloud(),
			Environment: computePool.Spec.Environment.GetId(),
			MaxCfu:      computePool.Spec.GetMaxCfu(),
			Region:      computePool.Spec.GetRegion(),
			CurrentCfu:  computePool.Status.GetCurrentCfu(),
			Status:      computePool.Status.GetPhase(),
			DefaultPool: computePool.Spec.GetDefaultPool(),
		}
		list.Add(out)
	}
	return list.Print()
}
